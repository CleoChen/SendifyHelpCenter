# Sendify 帮助中心

本目录是从公开的钉钉文档迁移而来的 Mintlify 文档项目。

## 本地预览

安装 Mintlify CLI 后，在项目根目录运行：

```bash
mint dev
```

## 发布

将本目录提交到 GitHub 仓库，并在 Mintlify 项目 `sendify-3a1f7fa4` 中连接该仓库。Mintlify 会读取根目录下的 `docs.json` 并部署文档。
